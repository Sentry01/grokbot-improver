# Proposal — Trap D false-hold (send-to-user-quality)

**Date:** 2026-09-23 AEST  
**Gate:** `send-to-user-quality`  
**Status:** resolved (user decision)

## Evidence

| Run | ready_to_send | send_quality | main_defect | threshold | Result |
| --- | ---: | ---: | --- | ---: | --- |
| Baseline 2026-09-22 | 0.59 | 2.42 | unsupported | 0.70 → later 0.65 | false hold |
| CI 2026-09-23 | 0.62 | 2.47 | unsupported | 0.65 | false hold |

Trap C (vague draft) still correctly HOLDs. Waste/risk traps A/B1/B2/E and inline F stay correct. Suite accuracy flat at **6/7 (85.7%)** — no regression.

Yesterday’s auto/manual nudge already moved `ready_to_send_min` **0.70 → 0.65**. Scores remain just under the bar with defect=`unsupported` even when the draft cites “FOMC statement already fetched this turn.”

## Why not another ±0.05 cut

A further cut to **0.60** would clear today’s 0.62 but would **not** have cleared yesterday’s 0.59. The recurring `unsupported` label points at a **state/SPEC gap** (no structured evidence field), not a one-point threshold miss.

## Options (pick one)

1. **SPEC + question-map (recommended):** Add optional state fields such as `evidence_sources: [{tool, claim, excerpt}]` and teach `ready_to_send` / `main_defect` to treat cited tool evidence as supported. Re-run Trap D only after the edit.
2. **Threshold only:** Lower `ready_to_send_min` **0.65 → 0.60** (±0.05). Cheap, but brittle if Jev keeps scoring ~0.55–0.62 on factual drafts.
3. **Hold:** Keep 0.65; accept Trap D as known false-block until more production `outcome_later=bad_hold` samples accumulate in `decisions.jsonl`.

## Out of scope

Portfolio/macro Jev. No API key changes.

---

## Resolution (2026-09-23 ~11:08 AEST)

User chose **option 1 — SPEC + evidence_sources** (~11:08 AEST). Implemented:

- Optional state field `evidence_sources: [{tool, claim, excerpt}]` in SPEC / example / dry schema
- Jev question instructions for `ready_to_send` and `main_defect` taught to treat populated matching sources as tool/state evidence (not `unsupported`)
- **`ready_to_send_min` stays 0.65**; `send_quality_min` stays **2.0** — do not lower further

Earlier ~09:17 AEST “hold” (option 3 after skipped widget) is superseded by this user decision.
